apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: directus-ingress
  namespace: directus-app
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`directus.eaglhandlaundry.com`)
      kind: Rule
      services:
        - name: directus
          port: 80
